# 语言: zh-CN
功能: 接口访问鉴权

  作为 API 网关
  我希望校验访问令牌并透传用户身份
  以便未授权请求无法访问受保护资源

  背景:
    假如 功能测试环境已就绪
    并且 数据库已初始化种子数据

  场景: 未携带令牌访问受保护接口返回 401
    当 未携带令牌访问 "/api/v1/me"
    那么 响应状态码为 401

  场景: 携带无效令牌访问受保护接口返回 401
    当 携带令牌 "not-a-valid-jwt-token" 访问 "/api/v1/me"
    那么 响应状态码为 401

  场景: 携带有效令牌可以访问当前用户信息
    当 用户 "admin" 使用密码 "Admin@123" 通过密码模式登录
    并且 携带登录访问令牌访问 "/api/v1/me"
    那么 响应状态码为 200
    并且 响应中的当前用户学工号为 "admin"
    并且 响应中的当前用户角色包含 "admin"

  场景: 登出后令牌被拉黑, 再次访问返回 401
    当 用户 "admin" 使用密码 "Admin@123" 通过密码模式登录
    并且 携带登录访问令牌调用登出接口
    那么 登出响应成功
    当 携带已登出的访问令牌访问 "/api/v1/me"
    那么 响应状态码为 401

  场景: 白名单接口无需令牌即可访问
    当 未携带令牌访问 "/health"
    那么 响应状态码为 200
